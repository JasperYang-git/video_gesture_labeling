# 项目梳理：离线摄像头手势动作分割（MS-TCN2）

> 面向接手者的项目说明：项目定位、端到端数据流、各模块职责、运行方式、接手建议与已知坑。

---

## 1. 这个项目是做什么的？

一句话概括：给定一段摄像头拍摄的手势视频，模型输出逐帧的动作类别序列（即时间轴上的动作分段），动作类别共 15 类（14 种手势 + background）。

• 任务类型：Temporal Action Segmentation（时序动作分割）/ 逐帧手势分类，离线处理（整段视频一次性输入，非实时流式）。

• 技术方案：MediaPipe 提取手部关键点特征 → MS-TCN2（多阶段时序卷积网络，1D 卷积，不需要 GPU 大显存）做逐帧预测。

• 动作类别（见 `data/mapping.txt`）：


| id | 动作 | id | 动作 |
|----|------|----|------|
| 0 | knock（敲） | 8 | snap（响指） |
| 1 | tap（点） | 9 | double-knock |
| 2 | slide-up | 10 | double-tap |
| 3 | slide-down | 11 | double-slide-up |
| 4 | clench（握拳） | 12 | double-slide-down |
| 5 | up-down | 13 | double-clench |
| 6 | wrist | 14 | background |
| 7 | nodded | | |

• 数据来源：`data/users/<user>/<视频文件夹>/`，每个文件夹包含一个 `.mp4` 和标注文件 `NOVA project/gestures.annotation~`（分号分隔：`time_begin;time_end;gesture_label;...`，时间单位为秒，label 为上表 id，-1 视为 14/background）。

• 视频文件夹命名有约定：形如 `ID_Sex_Side_...`，第 3 个字段（索引 3）是 `L`/`R`，用于左右手筛选（`HAND_SIDE='R'`）。

gestures.annotation示列如下：
DJI_20260609152055_LN023-bike1-2/gestures.annotation~
内容如下：第一列起始时间，第二列结束时间，第三列标签；第四列置信度，都设置为1可忽略。
0.0000;0.1333;10;1;
13.9333;14.6667;10;1;
18.3333;19.1333;10;1;
23.2667;24.0667;10;1;
26.6000;27.4000;10;1;
29.4667;30.2000;10;1;
32.6000;32.8667;10;1;
35.1333;35.9333;10;1;
38.6667;39.4667;10;1;
41.4667;42.2667;10;1;
43.9333;44.6667;10;1;
46.3333;47.1333;10;1;
48.2000;49.0000;10;1;
50.0000;50.8000;10;1;
54.4000;55.1333;10;1;
56.1333;56.8667;10;1;
57.6667;58.4667;10;1;
68.1333;68.7333;10;1;
69.9333;70.4667;10;1;
74.0667;74.8667;10;1;
77.2667;77.4667;12;1;
78.2667;78.9333;12;1;
87.5333;87.8667;12;1;
90.6667;91.3333;10;1;
92.4000;93.0000;12;1;
94.4667;94.9333;10;1;
95.8000;96.0667;10;1;
96.6667;97.3333;10;1;
99.2667;99.4000;10;1;
99.4667;99.5333;10;1;
99.6667;99.8000;10;1;
100.6667;101.2000;10;1;
101.3333;101.6000;10;1;
103.8000;104.5333;10;1;
107.4667;108.2667;12;1;
108.8000;109.6000;12;1;
110.7333;111.4667;10;1;
112.4000;113.1333;10;1;
118.1333;118.6000;10;1;
120.0000;120.7333;10;1;
122.2667;122.6000;10;1;
123.6667;124.4000;10;1;
126.9333;127.4000;10;1;
129.7333;130.4667;10;1;
131.2667;132.0000;10;1;
134.3333;135.0000;10;1;
135.6000;135.9333;10;1;
138.0000;138.8000;10;1;
142.4667;143.1333;12;1;
145.5333;146.0667;12;1;
148.0000;148.6667;12;1;
153.2000;153.6667;10;1;
158.4667;159.2000;10;1;
160.0000;160.6667;10;1;
162.6000;163.3333;10;1;
164.4000;165.0667;10;1;
166.4667;167.1333;10;1;




---

## 2. 目录结构

```
offlineCameraSeg_msTCN_alg/
├── main.py                     # 入口：数据检查 → 训练 / 预测（支持 accelerate 多卡）
├── train.sh                    # bash: python main.py --action=train
├── test.sh                     # bash: 预测 + 评估（注意：其中 eval 调用方式有 bug，见 §7）
├── setup.py                    # 打包为 MS_TCN_offline_cc_qh
├── configs/
│   └── pipeline_config.py      # 全局配置（路径、fps、窗口、阈值等）★改行为先看这里
├── dataset/                    # 数据预处理流水线（视频 → 特征/标签 → 数据集）
│   ├── run_pipeline.py         # 流水线调度入口 check_and_prepare_data()，main.py 启动时自动调用
│   ├── step1_extract_features.py   # Step1 调度：多进程跑 MediaPipe 特征提取 → temp_cache
│   ├── gesture_data_process.py # Step1 核心：MediaPipe 逐帧关键点 + One-Euro 平滑 + 下采样 → CSV
│   ├── step2_split_dataset_V3.py   # Step2 调度：质量审计 + 多进程滑窗切分 + 生成 .bundle 索引
│   └── boundary_process_then_split_data.py  # Step2 核心：滑动窗口裁剪 → features/*.npy + groundTruth/*.txt
├── engine/
│   ├── model_acc.py            # MS_TCN2 模型定义 + Trainer（训练/预测，accelerate 加速）
│   ├── batch_gen_acc.py        # BatchGenerator：读 .npy/.txt、打 batch、多卡数据分片
│   └── eval.py                 # 评估：Acc / Edit / F1@[0.1,0.25,0.5,0.6,0.7,0.8,0.9]
├── data/
│   ├── mapping.txt             # 动作 id ↔ 名称映射
│   ├── users/default/          # 原始数据（当前为空，需要自己放视频文件夹）
│   ├── temp_cache/             # Step1 中间缓存（每视频一个 CSV）
│   └── processed_dataset/      # 最终训练数据集（features/ groundTruth/ splits/ 审计报表）
└── experiments/                # 产出：models/ results/ logs/ evaluation_result/
```

---

## 3. 端到端数据流

```
原始数据 data/users/<user>/<视频文件夹>/{xxx.mp4, NOVA project/gestures.annotation~}
        │
        ▼  main.py 启动时 check_and_prepare_data() 检测到 processed_dataset 缺失则自动触发
┌─────────────────── Step 1: 特征提取 (step1_extract_features.py) ───────────────────┐
│ 多进程(70% CPU)逐视频:                                                              │
│  1. MediaPipe Hands 逐帧检测 → 21个关键点(x,y,z)，取置信度最高的那只手              │
│  2. 坐标以手腕(wrist)为中心归零 → 63 维；丢帧时沿用上一有效帧                        │
│  3. One-Euro Filter 平滑 63 维坐标（消除抖动）                                      │
│  4. 按原始 fps 下采样到 TARGET_FPS=15：窗口内坐标取均值并除以掌心距(palm size)归一化 │
│  5. 速度 = (当前归一化坐标 - 上一帧)/Δt → 63 维                                      │
│  6. 标注时间(秒)×fps 映射到帧 → 每个下采样帧的 gesture_label（窗口众数）             │
│  7. 质量审计字段: v_mask(窗口检出率>0.5), video_detection_rate(动作区间检出率),      │
│     min_label_info(最差类别检出率)                                                   │
│ 输出: data/temp_cache/temp_<视频ID>_<pid>/processed_<名>_offset_0.csv               │
│       (列: score, v_mask, c_0..c_62, v_0..v_62, gesture_label, 审计字段...)         │
└──────────────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────── Step 2: 审计与切分 (step2_split_dataset_V3.py) ─────────────────┐
│ 1. 只保留文件夹名第3段 == HAND_SIDE('R') 的视频                                      │
│ 2. 质量审计: video_detection_rate >= QUALITY_THRESHOLD(0.6) → PASSED / FILTERED     │
│    (报表落盘: processed_dataset/quality_audit_report.csv)                            │
│ 3. 滑动窗口切分(boundary_process_then_split_data.py):                               │
│    window_size=60帧(=4s @15fps), stride=60×(1-0.8)=12 帧(重叠采样)                  │
│    - 全背景窗口丢弃 (KEEP_BG_PROB=0)                                                │
│    - 尾部不足 60 帧 → 重复最后一帧 padding                                           │
│    - 特征矩阵按 [Channel, Time] 保存 (MS-TCN 输入规范)                               │
│ 输出: data/processed_dataset/                                                       │
│    ├── features/<视频ID>_off0_s<start>_<n>.npy   形状 [C, 60]                       │
│    ├── groundTruth/<同名>.txt                    60 行逐帧标签                       │
│    └── splits/{train,test}.split0.8.bundle      按"视频"8:2划分后的切片索引文件      │
└──────────────────────────────────────────────────────────────────────────────────┘
        │
        ▼  (--action=train)
┌─────────────────── 训练 (main.py + engine/) ──────────────────────────────────────┐
│ BatchGenerator: 读 train.split0.8.bundle → 同步 shuffle 后按 rank 分片(多卡各拿1/N)│
│   每个 batch: 加载 .npy/.txt, 对帧数补齐到 batch 内最长, 返回 (input[B,C,T],        │
│   target[B,T](padding=-100), mask[B,C,T])                                           │
│ Trainer: MS_TCN2 = PG(6层双向膨胀卷积残差) + 3 个 Refinement 阶段                   │
│   输出 predictions[num_stages+1, B, num_classes, T]                                  │
│   Loss = Σ(每个阶段的 CE + 0.1×TMSE平滑约束), accelerate.backward 自动多卡/混合精度│
│ 每个 epoch 保存 experiments/models/<user>/split_0.8/epoch-<N>.model                 │
│ 并输出 train_stats csv + loss/acc 曲线图                                             │
└──────────────────────────────────────────────────────────────────────────────────┘
        │
        ▼  (--action=predict)
┌─────────────────── 预测 (Trainer.predict) ────────────────────────────────────────┐
│ 逐个测试视频: 加载 epoch-<num_epochs>.model → 前向 → argmax → id 转回动作名         │
│ 输出: experiments/results/<user>/split_0.8/<视频名>                                  │
│   内容: "### Frame level recognition: ###\n<逐帧标签空格分隔>"                       │
└──────────────────────────────────────────────────────────────────────────────────┘
        │
        ▼  (python engine/eval.py)
┌─────────────────── 评估 (engine/eval.py) ─────────────────────────────────────────┐
│ 对 test.split0.8.bundle 中每个视频: 逐帧 Acc + Edit score + F1@{0.1..0.9}           │
│ 输出: experiments/evaluation_result/eval_<user>_split_0.8.csv                       │
└──────────────────────────────────────────────────────────────────────────────────┘
```

### 张量/数组形状速查

| 对象 | 形状 | 说明 |
|------|------|------|
| features .npy | `[C, T]` | C=特征维数（默认 127 或 128，见 §7 坑 1），T=60 |
| batch_input | `[B, C, T_max]` | batch 内按最长 T 补零 |
| batch_target | `[B, T_max]` | int64，padding 位置 = -100（CE 的 ignore_index） |
| mask | `[B, num_classes, T_max]` | 有效位置为 1，用于 TMSE |
| model 输出 | `[num_R+1, B, num_classes, T_max]` | PG 输出 + 3 个 Refinement 输出堆叠 |
| 最终预测 | `predictions[-1]` | 只取最后一个 Refinement 阶段的输出 |

---

## 4. 模型结构（MS_TCN2，engine/model_acc.py:17）

```
输入 [B, C, T]
  └─ Prediction_Generation (PG)
       1x1卷积升维到 num_f_maps=64
       6 个残差块，每块: [膨胀卷积(d=2^(5-i)) ∥ 膨胀卷积(d=2^i)] → concat → 1x1融合 → ReLU+Dropout+残差
       （双向膨胀卷积，感受野同时向前后扩展；MS-TCN++ 风格）
       1x1卷积输出 num_classes
  └─ Refinement ×3 (num_R)
       每阶段输入 = 上一阶段输出的 softmax
       1x1卷积 → 4 个 DilatedResidualLayer(d=1,2,4,8) → 1x1卷积输出 num_classes
```

• 损失：所有阶段输出求和。每个阶段 = `CrossEntropy(ignore_index=-100)` + `weight_smoothing_loss(0.1) × TMSE`（相邻帧 log_softmax 的 MSE，clamp 到 [0,16]，抑制逐帧抖动/碎片段）。

• 超参默认值：lr=5e-4 (Adam)，epochs=40，bz=2（每卡），num_f_maps=64，num_layers_PG=6，num_layers_R=4，num_R=3。


---

## 5. 关键配置（configs/pipeline_config.py）

| 配置项 | 默认值 | 含义 |
|--------|--------|------|
| `TARGET_FPS` | 15 | 特征下采样目标帧率 |
| `HAND_SIDE` | 'R' | 只保留文件夹名第 3 段为 R 的视频 |
| `MIN_DETECTION_CONFIDENCE` | 0.7 | MediaPipe 检测置信度阈值 |
| `QUALITY_THRESHOLD` | 0.6 | 动作区间检出率低于此值 → 整个视频被过滤 |
| `INCLUDE_V_MASK_IN_FEATURES` | False | 是否把 v_mask 加进特征（影响特征维数，见坑 1） |
| `WINDOW_SIZE` | 60 | 滑窗帧数（15fps 下 = 4 秒） |
| `STRIDE_PORTION` | 0.8 | 步长 = window×(1-0.8) = 12 帧，窗口重叠 80% |
| `KEEP_BG_PROB` | 0.0 | 纯背景窗口保留概率（0 = 全部丢弃） |
| `TRAIN_SPLIT_RATIO` | 0.8 | 按视频（不是切片）划分训练/测试，random_state=42 |
| `WHICH_USER_DATA` | "default" | 选择 `data/users/<user>` 数据子目录 |
| `CLEAN_TEMP` | False | 是否在切分后删除 temp_cache |
| `GENERATE_OFFSETS` | [0] | 下采样起始偏移（数据增强用，可加多个 offset 增样本） |

路径推导：`FINAL_DATASET_DIR = data/processed_dataset`，`TEMP_CACHE_DIR = data/temp_cache`，实验产出在 `experiments/`。

---

## 6. 如何运行

### 环境依赖
`setup.py` 未声明依赖，实际需要：`torch`、`accelerate`、`mediapipe`、`opencv-python`、`numpy`、`pandas`、`scikit-learn`、`loguru`、`matplotlib`。

### 完整流程
```bash
# 0. 放数据: data/users/default/<视频文件夹>/ 下放 *.mp4 和 "NOVA project/gestures.annotation~"
#    文件夹名需形如 <ID>_<Sex>_R_<...>（含 "_R_"）

# 1. 训练（首次运行会自动触发 Step1+Step2 数据处理，之后检测到数据集存在则跳过）
bash train.sh          # = python main.py --action=train
# 多卡: accelerate launch main.py --action=train

# 2. 预测（加载 epoch-40.model，与训练的 --num_epochs 一致）
python main.py --action=predict

# 3. 评估
python engine/eval.py
```

### 常用参数覆盖
```bash
python main.py --action=train --bz 8 --lr 0.0005 --num_epochs 80 \
    --weight_smoothing_loss 0.1 --split_ratio 0.8
```

---

## 7. 接手开发建议与已知的坑

### 已知的坑（接手后大概率会撞上）

1. 特征维数 127 vs 128 不一致（潜在运行时错误）
   `boundary_process_then_split_data.py:55-62` 实际特征列 = score(1) + c_0..62(63) + v_0..62(63) = 127 列（`INCLUDE_V_MASK_IN_FEATURES=False` 时不加 v_mask）。
   但 `main.py:37` 的 `--features_dim` 默认 128，且 `pipeline_config.py:19` 的注释（False→128）算错了。
   后果：模型第一层 `Conv1d(128, ...)` 收到 127 通道输入会直接报 shape 错误。
   修法（二选一）：把 `INCLUDE_V_MASK_IN_FEATURES` 改为 `True`（特征 128 维，与 main.py 一致，且把"检出掩码"作为先验喂给模型）；或把 `--features_dim` 改为 127。

2. test.sh 里评估命令是错的
   `python -m engine.eval.py` 会报错（`-m` 模式不带 `.py` 后缀）。应改为 `python engine/eval.py` 或 `python -m engine.eval`。

3. 改了预处理配置后必须清缓存
   `check_and_prepare_data()` 只检查 `processed_dataset/{features,groundTruth,splits}` 是否非空；且 Step1 检测到 `temp_cache/temp_<id>_*` 已有 CSV 会直接跳过。
   改了 `TARGET_FPS`/`WINDOW_SIZE`/平滑参数/特征列后，需要手动删除 `data/temp_cache/` 和 `data/processed_dataset/` 再重跑，否则用的还是旧数据。

4. 多卡训练的统计只有主进程的
   `BatchGenerator.read_data` 把数据按 `full_list[rank::num_replicas]` 分片，Trainer 里 loss/acc 只统计主进程自己那份 shard，没有 gather。单卡无影响；多卡时日志的 loss/acc 只反映卡 0 的数据，参考意义打折（train_stats 曲线同理）。

5. 评估对象是"滑窗切片"而非完整视频
   由于 Step2 按窗口切分，test 集里每条样本是一个 60 帧的切片，Acc/Edit/F1 都是切片级别的。若要看"整段视频连续分割"的效果，需要自己把同一原始视频的切片按 `s<start>` 偏移拼回去再评估。

6. 左右手其实没有真正区分
   `HAND_SIDE` 只作用于文件夹命名过滤，MediaPipe 提特征时取的是"置信度最高的手"，并未判断该手是左手还是右手。若视频里两只手同时出现，特征可能来自任意一只。

### 开发入手路径（建议顺序）

1. 跑通最小闭环：放入少量视频 → `python main.py --action=train`（它会自动完成数据预处理）→ `python main.py --action=predict` → `python engine/eval.py`。全链路打通后再谈改进。
2. 看懂一个样本：随便打开 `processed_dataset/features/xxx.npy`（形状 [C,60]）和对应 `groundTruth/xxx.txt`，对照 §3 理解每列含义。
3. 调参先动这三处：`configs/pipeline_config.py`（数据侧）、`main.py` 的 argparse 默认值（训练侧）、`engine/model_acc.py` 的模型结构（模型侧）。
4. 迭代数据质量：关注 `processed_dataset/quality_audit_report.csv`，FILTERED 的视频是检出率不达标的；如果误杀过多，调 `QUALITY_THRESHOLD`。
5. 可能的改进方向（供参考）：
   • 修复坑 1 后实验 `INCLUDE_V_MASK_IN_FEATURES=True`（把检测质量信息给模型）；

   • `GENERATE_OFFSETS` 设为 `[0, 4, 8]` 之类做下采样偏移增强，扩充训练样本；

   • 把 `KEEP_BG_PROB` 从 0 提到 0.1~0.3，让模型见过纯背景，减少背景误检为动作；

   • 评估脚本增加"按原始视频拼接后"的整段指标；

   • 多卡训练补上 metrics 的 `accelerator.gather`。


### 需要注意的代码习惯
• 全项目配置集中在 `pipeline_config.py` 的 `config` dict，路径均由 `PROJECT_ROOT` 绝对路径推导，换机器/换用户数据基本只改 `WHICH_USER_DATA`。

• 数据预处理大量使用 `multiprocessing.Pool`，日志是 print/imap_unordered 混合，乱序属正常。

• 训练日志用 loguru 写到 `experiments/logs/<dataset>_<split>_<time>.log`（仅主进程添加 handler）。